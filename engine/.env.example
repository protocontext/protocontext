# ProtoContext Engine — Environment Variables
# Copy this to .env and change the values

# Typesense API key — CHANGE THIS to a strong random value in production
# Generate one with: openssl rand -hex 32
TYPESENSE_API_KEY=protocontext-dev-key

# Uncomment for legacy auth mode (skips setup/login, uses static token)
# PROTO_API_TOKEN=your-secret-token-here
